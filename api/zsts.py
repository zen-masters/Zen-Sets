
def assign_to_group(identifier="", group_name="", group_color=(0, 0, 0), group_indices=[], group_mode='SELECTED') -> set:
    """_summary_

    Args:
        identifier (str, optional): _description_. Defaults to "".
        group_name (str, optional): _description_. Defaults to "".
        group_color (tuple, optional): _description_. Defaults to (0, 0, 0).
        group_indices (list, optional): _description_. Defaults to [].
        group_mode (str, optional): _description_. Defaults to 'SELECTED'.

    Returns:
        set: _description_
    """

    return {'FINISHED'}
